var React = require('react');

module.exports = React.createClass({

  render: function () {
    return(
      <div>
        Slider
        <div className="slider">.</div>
      </div>
    );
  }
});
